<h1>マイページ</h1>
<p><%= current_user.name %>さん、ようこそ</p>
<p>
   <strong>Photo:</strong>
   <% if @user.photo.attached? %>
   <%= image_tag @user.photo %>
   <% end %>
</p>
<p><%= link_to '編集', edit_user_path(current_user) %></P>
<p><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, data: { turbo_method: :delete } %></p>
<p><%= link_to 'ユーザー一覧', users_path %></p>
